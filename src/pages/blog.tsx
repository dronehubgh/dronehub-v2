import React from 'react';
import { BlogBanner, BlogSlider } from '../features';

const BlogPage = () => {
  return (
    <div>
      <BlogBanner />
      <BlogSlider />
    </div>
  );
};

export default BlogPage;
